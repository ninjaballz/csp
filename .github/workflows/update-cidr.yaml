name: Update Clean CIDRs

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      countries:
        description: 'Comma-separated country codes (e.g., US,GB,DE)'
        required: false
        default: 'US,GB,DE'
      total_cidrs:
        description: 'Total number of CIDRs to generate'
        required: false
        default: '30'

jobs:
  update-cidrs:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        
    - name: Update CIDR ranges
      env:
        COUNTRIES: ${{ github.event.inputs.countries || 'US,GB,DE,MY' }}
        TOTAL_CIDRS: ${{ github.event.inputs.total_cidrs || '50' }}
      run: |
        python /scripts/check.py --countries "$COUNTRIES" --total-cidrs "$TOTAL_CIDRS" --quiet
        
    - name: Check if files changed
      id: verify-changed-files
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
        else
          echo "changed=false" >> $GITHUB_OUTPUT
        fi
        
    - name: Commit and push changes
      if: steps.verify-changed-files.outputs.changed == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add cidr-ranges.txt clean-report.json
        git commit -m "🤖 Auto-update clean CIDRs for SMTP sending"
        git push
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: clean-cidrs
        path: |
          cidr-ranges.txt
          clean-report.json
        retention-days: 30
